name: 🎧 Automated Podcast Updater (Jekyll)

# 触发条件：
on:
  schedule:
    - cron: '0 8 * * 5' # 每周五 8:00 UTC 运行
  workflow_dispatch:

# 🚨 【关键新增部分】全局授予 Action 写入代码的权限
permissions:
  contents: write

jobs:
  update_data:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Repository
        # 这里不需要手动设置 token 了，因为全局权限已经配置
        uses: actions/checkout@v4

      - name: 🐍 Setup Python 3
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: ⚙️ Install Python dependencies
        run: pip install -r requirements.txt

      - name: 🏃 Run Podcast Update Script
        # ... 这里省略了 run: python scripts/update_podcast_collection.py 等步骤 ...

      - name: 💾 Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Automated: Update podcast episodes via RSS feed 🎧'
          file_pattern: '_data/episodes.yml' 
          commit_author: 'Ray2024Ray <goodrui86@gmail.com>'